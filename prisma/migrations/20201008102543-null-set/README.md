# Migration `20201008102543-null-set`

This migration has been generated by kimjeongwonn at 10/8/2020, 10:25:43 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."User" ALTER COLUMN "firstName" DROP DEFAULT,
ALTER COLUMN "lastName" DROP DEFAULT,
ALTER COLUMN "bio" DROP DEFAULT,
ALTER COLUMN "profileImage" DROP DEFAULT
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200930085445..20201008102543-null-set
--- datamodel.dml
+++ datamodel.dml
@@ -2,23 +2,23 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id             String    @default(cuid()) @id
+  id             String    @id @default(cuid())
   username       String    @unique
   email          String    @unique
-  firstName      String?   @default("")
-  lastName       String?   @default("")
-  bio            String?   @default("")
-  profileImage   String?   @default("")
+  firstName      String?
+  lastName       String?
+  bio            String?
+  profileImage   String?
   loginSecret    String?
   followers      User[]    @relation("FollowRelation")
   followings     User[]    @relation("FollowRelation")
   posts          Post[]    @relation("PostList")
@@ -29,9 +29,9 @@
   signUpAt       DateTime  @default(now())
 }
 model Post {
-  id       Int       @default(autoincrement()) @id
+  id       Int       @id @default(autoincrement())
   location String?
   caption  String?
   files    File[]
   likes    User[]    @relation("LikeList")
@@ -41,9 +41,9 @@
   postAt   DateTime  @default(now())
 }
 model Comment {
-  id        Int      @default(autoincrement()) @id
+  id        Int      @id @default(autoincrement())
   text      String
   user      User     @relation(fields: [userId], references: [id])
   userId    String
   post      Post     @relation(fields: [postId], references: [id])
@@ -51,22 +51,22 @@
   commentAt DateTime @default(now())
 }
 model File {
-  id     Int    @default(autoincrement()) @id
+  id     Int    @id @default(autoincrement())
   url    String
   post   Post   @relation(fields: [postId], references: [id])
   postId Int
 }
 model Room {
-  id          Int       @default(autoincrement()) @id
+  id          Int       @id @default(autoincrement())
   participant User[]    @relation(references: [id])
   messages    Message[]
 }
 model Message {
-  id         Int      @default(autoincrement()) @id
+  id         Int      @id @default(autoincrement())
   text       String
   fromUser   User     @relation(fields: [fromUserId], references: [id])
   fromUserId String
   room       Room     @relation(fields: [roomId], references: [id])
```


