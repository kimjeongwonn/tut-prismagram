# Migration `20200901092254-4`

This migration has been generated by kimjeongwonn at 9/1/2020, 9:22:54 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `prismagram`.`Message` DROP COLUMN `timestamp`,
ADD COLUMN `timeStamp` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3)

ALTER TABLE `prismagram`.`User` DROP COLUMN `firstname`,
ADD COLUMN `firstName` varchar(191)  
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200826133518-name..20200901092254-4
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -13,19 +13,19 @@
 model User {
   id              Int       @default(autoincrement()) @id
   username        String    @unique
   email           String    @unique
-  firstname       String?
+  firstName       String?
   lastName        String?
   bio             String?
   followers       User[]    @relation("FollowRelation", references: [id])
   following       User[]    @relation("FollowRelation", references: [id])
   posts           Post[]
   likes           Like[]
   comments        Comment[]
   room            Room[]
-  sendmessages    Message[] @relation("ToUser")
-  receivemessages Message[] @relation("FromUser")
+  sendMessages    Message[] @relation("ToUser")
+  receiveMessages Message[] @relation("FromUser")
 }
 model Post {
   id       Int     @default(autoincrement()) @id
@@ -76,6 +76,6 @@
   toUser     User     @relation("ToUser", fields: [toUserId], references: [id])
   toUserId   Int
   room       Room     @relation(fields: [roomId], references: [id])
   roomId     Int
-  timestamp  DateTime @default(now())
+  timeStamp  DateTime @default(now())
 }
```


