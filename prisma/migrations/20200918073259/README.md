# Migration `20200918073259`

This migration has been generated by kimjeongwonn at 9/18/2020, 7:32:59 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."_RoomToUser" DROP CONSTRAINT "_RoomToUser_A_fkey"

ALTER TABLE "public"."_RoomToUser" DROP CONSTRAINT "_RoomToUser_B_fkey"

CREATE TABLE "public"."_chatRoom" (
"A" integer   NOT NULL ,
"B" text   NOT NULL 
)

CREATE UNIQUE INDEX "_chatRoom_AB_unique" ON "public"."_chatRoom"("A", "B")

CREATE INDEX "_chatRoom_B_index" ON "public"."_chatRoom"("B")

ALTER TABLE "public"."_chatRoom" ADD FOREIGN KEY ("A")REFERENCES "public"."Room"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_chatRoom" ADD FOREIGN KEY ("B")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."_RoomToUser"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200918062131-pogresql..20200918073259
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -22,9 +22,9 @@
   followings  User[]    @relation("FollowRelation")
   posts       Post[]    @relation("PostList")
   likes       Post[]    @relation("LikeList")
   comments    Comment[]
-  rooms       Room[]
+  rooms       Room[]    @relation("chatRoom")
   messages    Message[] @relation("SendRelation")
   signUpAt    DateTime  @default(now())
 }
@@ -58,9 +58,9 @@
 }
 model Room {
   id          Int       @default(autoincrement()) @id
-  participant User[]
+  participant User[]    @relation("chatRoom")
   messages    Message[]
 }
 model Message {
```


