# Migration `20200911043435`

This migration has been generated by kimjeongwonn at 9/11/2020, 4:34:35 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `prismagram`.`Comment` ADD COLUMN `commentAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3)

ALTER TABLE `prismagram`.`Post` ADD COLUMN `postAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3)

ALTER TABLE `prismagram`.`User` DROP COLUMN `password`,
ADD COLUMN `loginSecret` varchar(191)  
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200910084833..20200911043435
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -16,9 +16,9 @@
   email           String    @unique
   firstName       String?   @default("")
   lastName        String?   @default("")
   bio             String?   @default("")
-  password        String
+  loginSecret     String?
   followers       User[]    @relation("FollowRelation", references: [id])
   followings      User[]    @relation("FollowRelation", references: [id])
   posts           Post[]
   likes           Like[]
@@ -37,8 +37,9 @@
   likes    Like[]
   comments Comment[]
   user     User      @relation(fields: [userId], references: [id])
   userId   String
+  postAt   DateTime  @default(now())
 }
 model Like {
   id     Int    @default(autoincrement()) @id
@@ -48,14 +49,15 @@
   postId Int
 }
 model Comment {
-  id     Int    @default(autoincrement()) @id
-  text   String
-  user   User   @relation(fields: [userId], references: [id])
-  userId String
-  post   Post   @relation(fields: [postId], references: [id])
-  postId Int
+  id        Int      @default(autoincrement()) @id
+  text      String
+  user      User     @relation(fields: [userId], references: [id])
+  userId    String
+  post      Post     @relation(fields: [postId], references: [id])
+  postId    Int
+  commentAt DateTime @default(now())
 }
 model File {
   id     Int    @default(autoincrement()) @id
```


